{
  "name": "nexuspay",
  "version": "1.0.0",
  "description": "NexusPay - Universal Agent Payment Layer for USDC",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "demo": "node scripts/demo-transactions.js",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ethers": "^6.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.4"
  }
}
